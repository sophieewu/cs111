#include <iostream>
using namespace std;

int main(){
    bool isRestart = true;

    // Curly brackets are not required when there is only one statement in the statement block
    if (isRestart) {
        cout << "Your computer will restart automatically in 30 seconds." << endl;
    }
  
    return 0;
}
